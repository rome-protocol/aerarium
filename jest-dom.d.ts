// Augment vitest's `expect` with @testing-library/jest-dom matchers.
// Must remain a non-module declaration file (no top-level import/export)
// so the `declare module` augmentation is treated as global ambient.
/// <reference path="node_modules/@testing-library/jest-dom/types/vitest.d.ts" />
